hydra:
  sweeper:
    _target_: hydra_plugins.hydra_optuna_sweeper.optuna_sweeper.OptunaSweeper
    sampler:
      _target_: optuna.samplers.TPESampler
    direction: maximize
    study_name: ${project.run_prefix}
    n_trials: 50
    params:
      train.lr: interval(0.0001, 0.01)
      train.epochs: range(10, 25)
      train.batch_size: range(64, 256)

      # 🔁 DynamicModel sweep parameters
      # train.num_hidden_layers: choice(2, 3, 4)
      # train.channels: choice([32, 64], [32, 64, 128], [64, 128, 128])
      # train.downsample: choice([pool], [pool, strided], [pool, None, strided])
      # train.activation: choice([relu], [relu, leaky_relu], [relu, leaky_relu, elu])
      # train.fc_activation: choice(relu, leaky_relu)
      # train.fc_units: choice([128], [256], [256, 128])
      # train.use_fc_dropout: choice(true, false)